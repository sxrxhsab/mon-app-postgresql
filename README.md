# Application de Gestion - Version PostgreSQL

## 📋 Prérequis
- PostgreSQL 12+
- Python 3.8+
- pip

## 🚀 Installation

### 1. Installer PostgreSQL
```bash
# Sur Ubuntu/Debian
sudo apt-get install postgresql postgresql-contrib

# Sur macOS
brew install postgresql
```

### 2. Configurer l'application
Modifiez `app.py` avec vos paramètres PostgreSQL dans la section `db_config`.

### 3. Installer les dépendances
```bash
pip install -r requirements.txt
```

### 4. Lancer l'application
```bash
python app.py
```
