<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recevoir le planning par email</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            padding-top: 50px;
        }
        .email-card {
            max-width: 600px;
            margin: 0 auto;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }
        .email-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px 15px 0 0;
            padding: 30px;
            text-align: center;
        }
        .email-icon {
            font-size: 60px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card email-card">
            <div class="email-header">
                <div class="email-icon">📧</div>
                <h2>Recevoir votre planning par email</h2>
                <p class="mb-0">Saisissez votre adresse email pour recevoir votre planning des examens</p>
            </div>
            
            <div class="card-body p-5">
                {% if error %}
                    <div class="alert alert-danger">{{ error }}</div>
                {% endif %}
                
                {% if email and message %}
                    <!-- Affichage après envoi -->
                    <div class="alert alert-{{ 'success' if '✅' in message else 'warning' }}">
                        <h4 class="alert-heading">
                            {% if '✅' in message %}✅ Succès{% else %}⚠️ Attention{% endif %}
                        </h4>
                        {{ message|safe }}
                    </div>
                    
                    <div class="mb-4">
                        <h5>📋 Planning envoyé :</h5>
                        <div class="list-group">
                            {% for examen in examens %}
                            <div class="list-group-item">
                                <div class="d-flex w-100 justify-content-between">
                                    <h6 class="mb-1">{{ examen.matiere }}</h6>
                                    <small>{{ examen.date }}</small>
                                </div>
                                <p class="mb-1">
                                    <strong>Heure :</strong> {{ examen.heure }}<br>
                                    <strong>Salle :</strong> {{ examen.salle }}<br>
                                    <strong>Surveillant :</strong> {{ examen.surveillant }}
                                </p>
                            </div>
                            {% endfor %}
                        </div>
                    </div>
                    
                    <div class="text-center">
                        <a href="{{ url_for('planning_etudiant') }}" class="btn btn-outline-primary me-2">
                            ↩️ Retour au planning
                        </a>
                        <a href="{{ url_for('etudiant_dashboard') }}" class="btn btn-primary">
                            🏠 Tableau de bord
                        </a>
                    </div>
                    
                {% else %}
                    <!-- Formulaire d'entrée d'email -->
                    <form method="POST" action="{{ url_for('envoyer_rappels') }}">
                        <div class="mb-4">
                            <label for="email" class="form-label fw-bold">Adresse email</label>
                            <input type="email" class="form-control form-control-lg" 
                                   id="email" name="email" 
                                   placeholder="exemple@email.com" required
                                   value="{{ request.form.email if request.form }}">
                            <div class="form-text">
                                Nous enverrons votre planning à cette adresse immédiatement.
                            </div>
                        </div>
                        
                        <div class="mb-4">
                            <div class="form-check">
                                <input class="form-check-input" type="checkbox" id="rappel_veille" name="rappel_veille" checked>
                                <label class="form-check-label" for="rappel_veille">
                                    📅 Recevoir également un rappel la veille de chaque examen
                                </label>
                            </div>
                        </div>
                        
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg">
                                📨 Envoyer mon planning maintenant
                            </button>
                            <a href="{{ url_for('planning_etudiant') }}" class="btn btn-outline-secondary">
                                ↩️ Retour sans envoyer
                            </a>
                        </div>
                    </form>
                {% endif %}
            </div>
            
            <div class="card-footer text-center text-muted">
                <small>
                    Le planning contiendra toutes les dates, salles et informations importantes.<br>
                    Un email de confirmation vous sera envoyé immédiatement.
                </small>
            </div>
        </div>
        
        <!-- Informations supplémentaires -->
        <div class="text-center mt-4 text-muted">
            <small>
                💡 Conseil : Vérifiez votre dossier "Spam" si vous ne recevez pas l'email dans les 5 minutes.<br>
                Pour toute assistance : contact@votrefaculte.edu
            </small>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Focus sur le champ email
        document.addEventListener('DOMContentLoaded', function() {
            const emailInput = document.getElementById('email');
            if (emailInput) {
                emailInput.focus();
            }
            
            // Confirmation d'envoi
            const form = document.querySelector('form');
            if (form) {
                form.addEventListener('submit', function(e) {
                    const email = document.getElementById('email').value;
                    if (email && confirm(`Envoyer le planning à ${email} ?`)) {
                        // Afficher un indicateur de chargement
                        const btn = this.querySelector('button[type="submit"]');
                        btn.innerHTML = '<span class="spinner-border spinner-border-sm me-2"></span>Envoi en cours...';
                        btn.disabled = true;
                    } else {
                        e.preventDefault();
                    }
                });
            }
        });
    </script>
</body>
</html>